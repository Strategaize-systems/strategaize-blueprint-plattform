---
id: RPT-032
date: "2026-04-05"
skill: /qa
slice: null
feature: null
type: completion
status: completed
result: "PASS — LLM-Migration + Whisper-Upgrade live verifiziert"
title: "QA-Nachtrag: LLM-Migration Bedrock + Whisper Large-v3"
---

# QA-Nachtrag: LLM-Migration + Whisper Upgrade

## Bezug

Nachtrag zur Gesamt-QA V3 Phase 1 (RPT-030). Abdeckt die nach der Gesamt-QA durchgeführte LLM-Migration und Whisper-Upgrade.

## QA Scope

| Änderung | Alt | Neu |
|----------|-----|-----|
| LLM | Ollama + Qwen 2.5 14B (lokal, CPU) | Claude Sonnet 4.6 via AWS Bedrock (eu-central-1 Frankfurt) |
| Whisper | small (~1-2 GB) | large-v3 (~3 GB) |
| Docker | 12 Services (inkl. Ollama) | 11 Services (Ollama entfernt) |

## Result: PASS

Alle Änderungen live verifiziert und funktional.

## Prüfbereiche

### 1. LLM-Migration (Code)

| Prüfung | Status |
|---------|--------|
| `llm.ts`: Bedrock SDK statt Ollama REST | OK — `BedrockRuntimeClient` + `ConverseCommand` |
| System-Messages korrekt separiert (Bedrock Converse API erwartet `system` separat) | OK — filter + map |
| `chatWithLLM()` Interface unverändert (Messages + Options → String) | OK — keine Änderung an Aufrufstellen nötig |
| Alle Prompts unverändert (DE/EN/NL, 4 Prompt-Typen) | OK — nur Transport geändert, nicht Inhalt |
| `updateRunMemory()` nutzt `chatWithLLM()` → funktioniert automatisch mit Bedrock | OK |
| Keine Ollama-Referenzen mehr im Source-Code | OK — `grep ollama src/` = 0 Treffer |
| Keine hardcoded AWS Keys im Code | OK — rein über ENV-Variables |
| Error Handling: leere Responses | OK — `throw new Error("Bedrock: empty response")` |

### 2. Stub Detection

- TODO/FIXME/HACK: 0 Treffer in `llm.ts`
- console.log/debugger: 0 Treffer
- Hardcoded Credentials: 0 Treffer
- Empty handlers: 0 Treffer

### 3. Docker Compose

| Prüfung | Status |
|---------|--------|
| Ollama-Service entfernt | OK — kein `ollama:` Block mehr |
| `ollama-models` Volume entfernt | OK |
| Whisper `ASR_MODEL=large-v3` | OK |
| Keine verwaisten Referenzen auf Ollama | OK |

### 4. Live-Verifikation (Hetzner)

| Prüfung | Status |
|---------|--------|
| `docker ps | grep ollama` → 0 Treffer | OK — Ollama komplett weg |
| `docker ps | grep whisper` → 1 Container, Running | OK |
| `docker exec ... env | grep ASR_MODEL` → `large-v3` | OK |
| Chat-Nachricht im Mirror-Workspace → Sonnet 4.6 Antwort in < 5 Sekunden | OK — User bestätigt |
| Antwortqualität: kontextbezogen, präzise Rückfrage, korrekte Anrede | OK — deutlicher Qualitätssprung vs. Qwen |

### 5. ENV-Variables (Coolify)

| Variable | Status |
|----------|--------|
| `AWS_ACCESS_KEY_ID` | Gesetzt |
| `AWS_SECRET_ACCESS_KEY` | Gesetzt |
| `AWS_REGION` = `eu-central-1` | Gesetzt |
| `LLM_MODEL` = `eu.anthropic.claude-sonnet-4-6` | Gesetzt (Cross-Region Inference Profile) |
| `OLLAMA_URL` | Entfernt |

### 6. DSGVO-Bewertung

| Aspekt | Bewertung |
|--------|-----------|
| Datenverarbeitung | AWS Bedrock eu-central-1 (Frankfurt) — EU-Region |
| Trainingsdaten | Anthropic nutzt keine API-Inputs für Modell-Training |
| Whisper | Weiterhin lokal auf Hetzner — kein externer Dienst |
| Audio-Daten | Weiterhin nur In-Memory, keine Speicherung |

## Findings

Keine.

## Open Risks

- **Bedrock-Verfügbarkeit:** Bei AWS-Ausfall in eu-central-1 funktioniert der Chat nicht. Kein Fallback auf lokales LLM mehr. Akzeptables Risiko für die Testphase.
- **Kosten-Monitoring:** AWS Bedrock ist pay-per-use. Empfehlung: AWS Budget-Alert einrichten (z.B. bei $50/Monat). Kein Blocker.

## Readiness Assessment

Die LLM-Migration ist **vollständig und live verifiziert**. Zusammen mit RPT-030 (Gesamt-QA V3) ist V3 Phase 1 release-ready.

## Recommended Next Step

`/final-check` V3 → Release REL-006
