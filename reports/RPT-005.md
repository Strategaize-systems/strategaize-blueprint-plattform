---
id: RPT-005
date: "2026-03-26"
skill: /backend
slice: SLC-004
feature: FEAT-001
type: completion
status: completed
reviewOf: null
result: "3 DB Integrity Issues resolved — Append-only Trigger, tenant_id, FK RESTRICT"
title: "SLC-004 — DB Integrity Hardening"
---

## Zusammenfassung

SLC-004 abgeschlossen. 3 DB-Integrity-Issues behoben: Append-only Trigger (ISSUE-010), evidence_links tenant_id (ISSUE-014), FK RESTRICT (ISSUE-018).

## Micro-Tasks

### MT-1: Append-only Trigger (ISSUE-010)
- `prevent_modify()` Function erstellt
- BEFORE UPDATE OR DELETE Trigger auf 5 Tabellen: question_events, evidence_items, evidence_links, run_submissions, admin_events
- DB-Level Enforcement — wirkt auch bei BYPASSRLS

### MT-2: evidence_links tenant_id (ISSUE-014)
- Schema: `tenant_id uuid NOT NULL REFERENCES tenants ON DELETE RESTRICT` + Index
- RLS: SELECT/INSERT Policies vereinfacht auf direkte `tenant_id = auth.user_tenant_id()` Prüfung
- API: 3 INSERT-Stellen aktualisiert (evidence/link route + 2x evidence route)

### MT-3: questions FK RESTRICT (ISSUE-018)
- `ON DELETE CASCADE` → `ON DELETE RESTRICT` auf questions.catalog_snapshot_id

## Betroffene Dateien

### Geändert
- `/sql/functions.sql` — prevent_modify() Function + 5 Trigger
- `/sql/schema.sql` — evidence_links.tenant_id Spalte + Index, questions FK RESTRICT
- `/sql/rls.sql` — evidence_links Policies vereinfacht
- `/src/app/api/tenant/runs/[runId]/evidence/[evidenceId]/link/route.ts` — tenant_id beim INSERT
- `/src/app/api/tenant/runs/[runId]/evidence/route.ts` — tenant_id bei 2 INSERT-Stellen

## Production-Deployment
SQL-Änderungen müssen auf Production-DB angewendet werden:
1. prevent_modify() Function + 5 Trigger erstellen
2. evidence_links: ALTER TABLE ADD COLUMN tenant_id + Backfill + NOT NULL + Index
3. questions: ALTER TABLE DROP/ADD FK (CASCADE → RESTRICT)
4. evidence_links RLS Policies: DROP + CREATE (neue Versionen)
