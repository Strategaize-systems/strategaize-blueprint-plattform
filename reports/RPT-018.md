---
id: RPT-018
date: "2026-03-30"
skill: /qa
slice: SLC-027
feature: FEAT-018
type: completion
status: completed
reviewOf: null
result: "PASS — catalog language support implemented, TypeScript clean, migration ready"
title: "QA: SLC-027 Katalog-Sprache + Admin"
---

# QA Report: SLC-027 — Katalog-Sprache + Admin-Integration

## Ergebnis: PASS

## Prüfumfang

Code-Review aller geänderten Dateien. TypeScript-Check bestanden. Migration SQL geprüft.

## Geprüfte Dateien

| Datei | Änderung | Verifiziert |
|-------|----------|-------------|
| `sql/migrations/007_catalog_language.sql` | Migration: language column + UNIQUE constraint | ja |
| `src/lib/validations.ts` | language field in importCatalogSchema | ja |
| `src/app/api/admin/catalog/snapshots/route.ts` | GET+POST: language in select, insert, uniqueness check | ja |
| `src/app/api/admin/tenants/route.ts` | GET: language in select | ja |
| `src/app/admin/catalog/catalog-client.tsx` | Language badge + selector in import dialog | ja |
| `src/app/admin/runs/runs-client.tsx` | Catalog language display + tenant-matching sort | ja |
| `slices/SLC-027-catalog-language.md` | Slice definition updated to done | ja |

## Acceptance Criteria Check

| # | Kriterium | Status |
|---|-----------|--------|
| 1 | `question_catalog_snapshots.language` existiert | PASS (Migration bereit) |
| 2 | Admin kann Sprache beim Import angeben | PASS (Select-Dropdown im Dialog) |
| 3 | Gleiche Version in verschiedenen Sprachen möglich | PASS (UNIQUE(version, language)) |
| 4 | Katalog-Liste zeigt Sprach-Badge | PASS (LANGUAGE_LABELS/COLORS) |
| 5 | Run-Erstellung zeigt Katalogsprache + markiert passende | PASS (Sort + Checkmark) |
| 6 | Bestehende Kataloge bekommen language='de' | PASS (DEFAULT 'de') |
| 7 | TypeScript-Check | PASS (0 Fehler) |
| 8 | Migration SQL bereit | PASS |

## Findings

### Severity: Info — Migration muss auf Production ausgeführt werden

`sql/migrations/007_catalog_language.sql` muss via Supabase SQL Editor auf Production laufen. Die Migration ist idempotent-safe (DROP CONSTRAINT IF EXISTS).

### Severity: Low — Kein Auto-Select bei Run-Erstellung

Der Katalog wird bei Run-Erstellung nicht automatisch auf den passenden Sprach-Katalog vorausgewählt — der Admin muss manuell wählen (mit Sortierung als Hilfe). Das ist bewusst so: Admin soll die Kontrolle behalten.

### Severity: Info — Ebene-Feld nicht übersetzt

`ebene` ("Kern", "Workspace") bleibt als Identifier — wird nicht pro Sprache übersetzt. Akzeptabel für V1.1.

## Stub-Scan

Keine TODO/FIXME, keine Placeholder-Returns, keine console.log, keine Credentials in den geänderten Dateien.

## Nicht geprüft

- Migration auf Production (nur SQL-Review)
- Tatsächlicher Katalog-Import mit EN/NL-Daten (keine Testdaten vorhanden)
- Browser-Rendering der Admin-UI-Änderungen

## Fazit

SLC-027 ist vollständig. Katalog-Snapshots können jetzt mit Sprach-Tag importiert werden. Admin sieht Sprache in der Katalogliste und beim Run-Erstellen. Migration SQL ist bereit. BL-022 (Mehrsprachigkeit) ist damit komplett — alle 4 Slices done.
