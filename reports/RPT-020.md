---
id: RPT-020
date: "2026-03-30"
skill: /final-check
slice: null
feature: null
type: completion
status: completed
reviewOf: null
result: "CONDITIONALLY READY — 3 pre-deploy migrations required, no code blockers"
title: "Final Check: V1.1 Pre-Release Audit"
---

# Final Check: V1.1 Pre-Release Audit

## Overall Assessment: CONDITIONALLY READY

V1.1 ist release-ready unter der Bedingung, dass 3 DB-Migrationen (MIG-007, MIG-008, MIG-009) vor dem Redeploy auf Production ausgeführt werden. Kein Code-Blocker.

---

## 1. Code Quality & Maintainability

### Ergebnis: AKZEPTABEL

| Dimension | Status |
|-----------|--------|
| TypeScript strict | PASS — `tsc --noEmit` 0 Fehler |
| Codebase size | 8.089 LOC (43 TS/TSX-Dateien) — angemessen |
| Largest file | `run-workspace-client.tsx` (1.180 Zeilen) — groß, aber strukturiert |
| console.log/debug | 0 gefunden |
| console.error/warn | 4x in `logger.ts` — korrekt, das ist der Logger selbst |
| TODO/FIXME | 0 gefunden |
| Dead code | `SYSTEM_PROMPTS` export in llm.ts deprecated, kein Caller |
| Unused imports | Keine gefunden |

**Finding: Low** — `run-workspace-client.tsx` mit 1.180 Zeilen ist grenzwertig groß. Refactoring in Subkomponenten wäre V2-Scope. Funktional korrekt.

---

## 2. Security & Privacy

### Ergebnis: GUT

| Check | Status |
|-------|--------|
| Secrets in Repo | PASS — nur `.env.*.example` mit Platzhaltern |
| Hardcoded Credentials | PASS — alle Secrets via `process.env` |
| Auth Flow | PASS — Invite-only, Rate-Limiting auf Login/SetPassword |
| RLS (Row-Level Security) | PASS — 22+ Policies, Cross-Tenant-Isolation verifiziert |
| service_role Nutzung | PASS — nur in Admin-Routen (requireAdmin Guard) |
| CSRF | PASS — Server Actions + SameSite Cookies |
| Data Isolation | PASS — tenant_id auf allen relevanten Tabellen |
| Append-only Enforcement | PASS — DB-Trigger + RLS |
| Password Handling | PASS — GoTrue Auth, nie im Klartext |
| DSGVO | PASS — Alle Daten auf Hetzner (EU/DE), kein externer API-Call für Kundendaten |

**Finding: Info** — Kein Sentry/Error-Tracking im externen Sinne. Error-Logging geht in eigene DB-Tabelle + E-Mail-Alerts. Ausreichend für V1.1.

---

## 3. Legal & Compliance

### Ergebnis: AKZEPTABEL

| Check | Status |
|-------|--------|
| DSGVO-Konformität | Daten bleiben auf Hetzner (DE). Kein LLM-Cloud-Call. |
| Dependencies Lizenzen | Alle MIT/ISC/Apache-2.0 — keine GPL-Risiken |
| Datenschutz-Seite | NICHT VORHANDEN — für B2B-Invite-Only akzeptabel, vor öffentlichem Launch nötig |
| Impressum | NICHT VORHANDEN — gleiche Bewertung |

**Finding: Low** — Datenschutz/Impressum fehlen. Für geschlossene B2B-Plattform (Invite-only) akzeptabel. Muss vor öffentlichem Zugang ergänzt werden.

---

## 4. Testing Depth

### Ergebnis: BEKANNTES RISIKO

| Check | Status |
|-------|--------|
| Unit Tests | KEINE — ISSUE-002/020 (open) |
| Integration Tests | KEINE |
| E2E Tests | KEINE |
| TypeScript strict | PASS |
| QA Reports | 19 Reports (RPT-001 bis RPT-019) |
| Manuelle QA | Umfangreich (Slice-QA + Gesamt-QA) |

**Finding: Medium** — Keine automatisierten Tests. Bekanntes und dokumentiertes Risiko (ISSUE-002, ISSUE-020). Manuell kompensiert durch 19 QA-Reports. Kein Blocker für V1.1, aber Test-Framework (vitest) sollte V2-Priorität sein.

---

## 5. Deployment Readiness

### Ergebnis: BEREIT (nach Migrationen)

| Check | Status |
|-------|--------|
| Dockerfile | PASS — Multi-stage Alpine Build, standalone output |
| .env Dokumentation | PASS — `.env.deploy.example` mit allen Vars |
| Dual-URL-Strategie | PASS — SUPABASE_URL (intern) vs NEXT_PUBLIC_SUPABASE_URL (extern) |
| NEXT_PUBLIC_* Build-Time | PASS — im Dockerfile korrekt verfügbar |
| Docker-interne URLs | PASS — `http://supabase-kong:8000`, `http://ollama:11434` |
| Coolify/Caddy Routing | PASS (bestehende Infrastruktur) |
| SSL/TLS | PASS (Coolify Auto-HTTPS) |

### Ausstehende Migrationen (PFLICHT vor Redeploy)

| Migration | SQL-Datei | Risiko |
|-----------|-----------|--------|
| MIG-007 | `sql/migrations/007_catalog_language.sql` — war falsch benannt, ist `question_events` CHECK | Minimal |
| MIG-008 | Inline — `tenants.language` Spalte | Minimal (DEFAULT 'de') |
| MIG-009 | `sql/migrations/007_catalog_language.sql` — `catalog_snapshots.language` | Minimal (DEFAULT 'de') |

**Wichtig:** MIG-007 (question_events CHECK) und MIG-008 (tenants.language) wurden ggf. schon bei SLC-020/SLC-024 auf Production ausgeführt. Vor Ausführung mit `\d question_events` und `\d tenants` prüfen ob die Spalten/Constraints bereits existieren.

**MIG-009** ist definitiv NEU und muss ausgeführt werden.

**Reihenfolge:** Erst alle Migrationen → dann Redeploy.

**Finding: Info** — Bei Redeploy muss Ollama vorher gestoppt werden (ISSUE-024, Build OOM). Workflow: `docker stop ollama` → Coolify Build → `docker start ollama`.

---

## 6. Observability & Operations

### Ergebnis: AKZEPTABEL

| Check | Status |
|-------|--------|
| Error Logging | PASS — eigene `error_log` Tabelle + captureException() |
| Error Alerts | PASS — E-Mail bei Errors (BL-017) |
| Structured Logging | PASS — `logger.ts` mit source, message, context |
| Health Checks | Docker HEALTHCHECK im Compose |
| Backup | Hetzner Snapshots (manuell) |
| Cost Monitoring | Hetzner Dashboard |

---

## 7. Post-Go-Live Readiness

### Ergebnis: GUT

| Check | Status |
|-------|--------|
| Release Notes | RELEASES.md mit REL-001 (MVP-1) |
| Known Issues | 26 Issues dokumentiert, 21 resolved, 3 open, 2 deferred |
| Rollback Plan | Coolify Rollback auf vorherigen Container |
| Migration Rollback | Alle Migrationen haben dokumentierte Rollback-SQL |
| Incident Response | Error Alerts via E-Mail |

---

## Findings Summary

### Blocker
Keine.

### High
Keine.

### Medium
1. **Keine automatisierten Tests** (ISSUE-002/020) — bekannt, dokumentiert, manuell kompensiert. V2-Priorität.

### Low
1. **`run-workspace-client.tsx` zu groß** (1.180 LOC) — funktional korrekt, Refactoring V2
2. **Datenschutz/Impressum fehlen** — für B2B Invite-only akzeptabel
3. **Deprecated `SYSTEM_PROMPTS` Export** — Dead Code, kann entfernt werden

### Info
1. **3 DB-Migrationen vor Redeploy** (MIG-007/008/009) — PFLICHT
2. **Ollama stoppen vor Build** (OOM Workaround)
3. **Admin-UI bleibt Deutsch** — by design

---

## Pre-Deploy Checklist

- [ ] MIG-007 auf Production prüfen/ausführen (question_events CHECK)
- [ ] MIG-008 auf Production prüfen/ausführen (tenants.language)
- [ ] MIG-009 auf Production ausführen (catalog_snapshots.language)
- [ ] Ollama Container stoppen
- [ ] Coolify Redeploy triggern
- [ ] Ollama Container starten
- [ ] Smoke-Test: Login, Dashboard, Workspace, Sprachenwechsel

---

## Release Risks

| Risiko | Likelihood | Impact | Mitigation |
|--------|------------|--------|------------|
| Build OOM durch Ollama | Hoch | Deploy schlägt fehl | Ollama vorher stoppen |
| Migration-Reihenfolge falsch | Niedrig | App-Fehler bei Start | Migrationen vor Redeploy |
| Sprachenwechsel nicht getestet | Mittel | UI-Strings nicht sichtbar | Smoke-Test nach Deploy |
| Keine Tests = Regression | Mittel | Unbekannt | 19 QA-Reports, manueller Smoke-Test |

---

## Empfehlung

**Conditionally ready for `/go-live`.** Bedingung: Migrationen auf Production ausführen.

Nächster Schritt: `/go-live` → dann `/deploy` mit Migration + Redeploy.
